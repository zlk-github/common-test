package com.zlk.db.manager;

/**
* @author  likuan.zhou
* @title:  IUserManager
* @description: 用户Manager (Manager层一般处于service与mapper之前，用于处理事务。当前没使用)
* @date 2021-09-14
*/
public interface IUserManager{
    //Manager层一般处于service与mapper之前，用于处理事务。当前没使用
}
